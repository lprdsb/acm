#include<bits/stdc++.h>

const int N = 1000;
long long a[N];

int main() {
    for(int i = 0;i < N;i++) {
        scanf("%lld", &a[i]);
    }
    for(int i = 0;i < N;i++) {
        printf("0");
    }
    return 0;
}